#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * @argc: count of the arguments
 * @argv: array of pointers to the string
 * Return: always 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
